<table>
    <thead>
        <tr>
            <th data-field="id">Essen</th>
            <th data-field="id">Preise</th>
        </tr>
    </thead>

    <tbody>
    {{#each foods}}
        <tr>
            <td>{{name}}</td>
            <td>{{price}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>
